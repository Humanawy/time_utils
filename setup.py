from setuptools import setup, find_packages

setup(
    name="gridtime",
    version="0.2.0",
    packages=find_packages(),
    install_requires=[],
    author="Kacper",
    description="Moduł do pracy z jednostkami czasu (godziny, dni, miesiące, itd.)",
    python_requires=">=3.7",
)
